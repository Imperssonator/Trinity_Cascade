VFOs = zeros(3,10);

startNS = 0.8;
endNS = 0.4;
NScomps = linspace(0.4,0.8,10);

for i = 1:10
    VFOs(2,i) = NScomps(i);
    VFOs(1,i) = (1-VFOs(2,i))/2;
    VFOs(3,i) = 1-VFOs(1,i)-VFOs(2,i);
end

